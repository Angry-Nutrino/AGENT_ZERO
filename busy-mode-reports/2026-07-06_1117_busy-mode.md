# Busy-Mode Session — Mon 06 Jul 2026, 11:17 IST
**Status:** Ended (graceful stop — Alkama: "stop busy-mode after the next task", 22:35 IST)
**Started:** Mon 06 Jul 2026, 11:17 IST
**Ended:** Mon 06 Jul 2026, ~23:05 IST — 11 tasks: schedule ritual, 2 drills (06m INFRA casualty handled
+ 06e 22/22 clean with G15 firing live), G15 + G16 guards built+validated, long_session + off_rhythm A2
markers built (26 tests), remark character + anti-fabrication backstop, G14 leak mitigated + BRIEF_55,
G4 + G7 verifier items fully closed, commit draft refreshed. Zero open breakage; stack stopped.

Fifth lifetime. Triggered by Alkama with a 2-minute plan-preview ("Get along with me for what all will you
do") — he approved the announced plan implicitly by leaving; he is away all day (remote y/n improbable).
Announced plan: (1) 06jul schedule file + 05jul reconcile, (2) drill 06-morning, (3) G15 date_time
completeness guard, (4) long_session build (dormant), (5) commit-message refresh + upkeep. HIS items staged
but untouchable by me: Stephen call check, partner A re-registration, LinkedIn sends ×3.
Orientation: fully current in-context (same session as the 07-05 drill + BACKLOG/TIMELINE updates minutes
ago); no Ongoing report existed. Cron margin: 11:17 vs 20:00 — safe.

---

## Task 1 — Daily-schedule duty: reconcile 05jul + write 06jul (morning fallback)

**Which & why (before):** First because it's the system's own contract (morning fallback at first contact —
no 06jul file exists) and because today's file is the artifact Alkama will glance at from his phone: it must
carry HIS two Monday-critical items (Stephen call confirm/time check + partner A re-registration) and the
3 LinkedIn sends, or the day's external commitments slip silently. 🟢, no risk, S-size. Reconciliation of
05jul from observed state: drill Top-3 #1 was DONE in-session (5 reports incl. the rotation verdict + the
Q21 real FAIL → G15); LinkedIn sends NOT observed sent; BRIEF_53 decision NOT observed (rolled×3 → forces
do/delegate/drop per the rot rule — I'll surface that choice prominently, defaulting to DELEGATE-with-my-
recommendation as the least-blocking option he can veto); commit+push NOT done (rolled×3 → same treatment;
it needs HIM anyway, I only refresh the message); wrap-the-day didn't happen (he vanished — recorded
plainly, zero guilt mechanics). Books/gym/personal: unknown → left blank for his correction. Edge cases:
don't fabricate his-side completions; Stephen call TIME is unknown to me (LinkedIn is his surface) — the
file says "check thread" rather than inventing a slot.

*(outcome below)*
**Outcome — Mon 06 Jul, 11:22 — DONE.** 05jul day-close appended (drill=done✓, BRIEF_53 & commit hit
rolled×3 → rot-rule surfaced as forced one-line choices in today's file; his-side blanks left blank).
06jul written: Top-3 = his three untouchables (Stephen thread check — time UNCONFIRMED on record, so the
file says check-then-confirm, never invents a slot; partner A §7.2 checklist; the two rot-rule calls), my
busy-mode lane listed for transparency. Files: My_Schedule/{05,06}jul_Schedule.md.

---

## Task 2 — G1 · Drill: 2026-07-06-morning (third rotated-morning run)

**Which & why (before):** Drill is the standing top-🟢 when pending, and this one has extra signal: third
run of the rotated morning set AND the first cron after yesterday's Q21-evening FAIL — so I specifically
check (a) whether the morning set stays clean, (b) whether mQ21/mQ22 (same datetime R3 family as the failed
eQ21) show the formatter-condensation class, (c) Layer-2's gold seed after last night's bad run (2nd-ever
mismatch — is it a trend?). Protocol as always: scorecard-anchored, every FAIL ground-truthed via session
log before judging Clara; analyses are the durable record. S-size, no risk.

**Outcome — Mon 06 Jul, 11:50 — DONE, and it was an emergency, handled.** 06-morning = **INFRA CASUALTY:
DeepSeek 402 Insufficient Balance** on all 22 questions (cron also fired late at 11:06 — laptop asleep at
08:00, R15 class). Actions: (1) **Telegram alert to Alkama 11:35** (only he can top up; tonight's cron at
risk too); (2) analysis written classifying the run INFRA — Clara's record untouched (13/14 clean stands);
(3) the 14 polluted fail_counts **restored** to post-05m state from the 05m ladder snapshot (a question
never answered cannot carry a fail); (4) **G16 BUILT + LIVE-VALIDATED**: `api_is_usable()` pre-flight in
test_harness.py — one minimal completion after the reachability check; 402/401 → clean skip + Telegram, no
spawn, no report, no pollution; transient errors deliberately proceed. Validated against the REAL live 402.
Fork decision logged: sending the Telegram alert autonomously = the notifier's designed purpose (system
tells its owner it's out of fuel), judged in-contract. Files: tests/test_harness.py,
tests/questions_morning.json (restore), reports/2026-07-06-morning.md, My_Schedule/06jul (Top-3 amended).

---

## Task 3 — G15 · date_time completeness guard (from the 05e Q21 real FAIL)

**Which & why (before):** Next because it's the one REAL failure class currently open (format_llm condensed
a complete date_time result to "Wednesday.", dropping the demanded date) and it's small, contained, and
pattern-proven — it mirrors the existing numeric-fidelity guard in `_run_fast` exactly (same location, same
return-raw fallback). 🟡-contained; blast radius = one guard clause on the FAST format path for ONE tool.
Constraint acknowledged: **DeepSeek is down (402), so no live boot-test is possible** — validation =
deterministic unit test of the guard function + ast/self-checks; the live-fire debt is explicitly noted for
after top-up (next drill run exercises it naturally: eQ21 has fail_count=1 and reruns tonight/tomorrow).
Edge cases: don't over-trigger (a legitimate reframe like "Wednesday, 10 June 2026" reworded must pass);
only enforce when the question/result carries a computed offset block; never touch other tools' formatting.

**Outcome — Mon 06 Jul, 12:05 — DONE.** `_date_completeness_ok` helper (module-level, testable) + the
guard in `_run_fast` beside the numeric guard, scoped to date_time computed-offset lines only. Regression
test `tests/test_date_completeness.py`: **8/8** incl. the exact 05e Q21 case (guard fires), legit-rephrase
no-over-trigger, and completeness-not-correctness boundary. agent.py parses. **Live-fire debt CLEARED
early:** Alkama topped up DeepSeek at ~12:00 (responding to the 11:35 Telegram alert — the remote-alert
loop WORKED); API verified usable again. Live validation of the guard rides tonight's cron (eQ21 reruns
with fail_count=1). Files: core_logic/agent.py, tests/test_date_completeness.py (gitignored per R11).

---

## Task 4 — Y · long_session marker (the agreed first A2 signal-set build)

**Which & why (before):** The explicitly-agreed next build from the 04-Jul design session ("for building
long session now, yes... it's completely deterministic"). Algorithm was fixed on paper with Alkama:
session = continuous foreground activity from A0's timeline; survives idle gaps < 15 min (break-tolerance);
a gap ≥ 15 min ends it; FIRE at 150 min continuous, once per session; nudge names the dominant foreground
app. Both knobs proposed as tunable defaults (he never objected; they ship as env-tunable). Tier 🟡:
additive classifier + salience entries + a session-tracker over A0 records — contained, deterministic,
fully self-testable offline; ships through the EXISTING A2 gate/loop (cooldowns, ledger, LLM composer all
reuse). Dormant-ish by nature: A2_MODE already gates surfacing; long_session adds a class, no new
arming surface. Edge cases: midnight-spanning sessions (duration math over date boundary); sparse A0
sampling (records only on window CHANGE — a 3h unbroken VS Code session may be ONE record → duration must
derive from record-to-now spans, not record counts); idle detection via session_rhythm events interleaved;
re-fire suppression per session; baseline absence (fresh store). Validation: self-test with synthetic
timelines (long clean session fires once; 15-min gap resets; short sessions never fire; midnight span).

**Outcome — Mon 06 Jul, ~13:10 — DONE.** `detect_long_session()` + local `_iso_to_epoch` in
`core_logic/salience.py` (kept salience pure — no ambient_loop import; circular-import trap caught and
fixed during build); tick() integration + composer branch in `core_logic/ambient_loop.py` (same
gate→dedup→ledger→emit path as the other classes, wrapped so a marker bug can never kill the loop). The
sparse-A0 trap is the design center and is LOCKED IN BY TEST: a 3h unbroken session is ONE record, so
duration derives from record-to-now spans and only a session_rhythm idle ≥ tolerance breaks a session.
`tests/test_long_session.py`: **11/11** (sparse single-record fires; 100-min no-fire; 20-min gap resets
then re-fires; 6-min tea pause survives; currently-away never nudged; dominant-app by held-span; midnight
span; gate SURFACEs at exactly 0.5). salience + ambient_loop module self-tests green. **Boot-validated
live** (post-top-up): backend up, log shows "A2 ambient loop started (A2_MODE=live)" with zero ambient
errors, sanity `POST /query` (memory_mode=none) → "5040." — pipeline healthy on the refilled API. Backend
stopped after, boot log removed. Knobs: `LONG_SESSION_TRIGGER_MIN` (150) / `LONG_SESSION_BREAK_MIN` (15) in
.env if retuning is ever wanted. Files: core_logic/salience.py, core_logic/ambient_loop.py,
tests/test_long_session.py (gitignored per R11). Next A2 build per the agreed order: off_rhythm (designed,
sizeable), then the screenshot pipeline (vision-backend decision still parked with Alkama).

---

## Task 5 — Commit-message refresh + window consolidation (G9)

**Which & why (before):** Last planned item of the announced five. The uncommitted tracked surface has
grown all window (agent.py G15, test_harness.py G16, ambient_loop.py long_session, questions restore,
TIMELINE/BACKLOG, plus everything staged from prior days — 20+ tracked files) and the standing contract is
that Alkama commits from MY draft; a stale draft is worse than none because he'd under-describe the diff.
Also the consolidation duties: verify `git check-ignore` still covers AGENT_ZERO_PRIVATE/ + My_Schedule/
(public repo — the one non-negotiable), list open briefs awaiting him, and close this report. 🟢, S-size,
pure documentation — no code risk. Edge cases: the commit message must end at the last content line (no
Co-Authored-By/AI attribution, hard rule); untracked-but-gitignored files must NOT appear in the draft;
new untracked REPORT files (07-02e→07-06m) are tracked-by-policy and belong in it.

**Outcome — Mon 06 Jul, ~13:25 — DONE.** `git check-ignore` verified on all five sensitive paths
(AGENT_ZERO_PRIVATE/, My_Schedule/, core_logic/.env, tests/, briefs/ — all covered, output logged).
Surface: 13 tracked-modified + 10 untracked-by-policy files, 983 insertions. Drafts below.

---

## Consolidation checkpoint — Mon 06 Jul 2026, ~13:25 IST (loop still Ongoing)

**Lifetime so far:** 5 tasks done — (1) 05jul reconcile + 06jul schedule; (2) drill 06m = INFRA casualty
(DeepSeek 402) → Telegram alert → Alkama topped up → fail_counts restored; (3) G15 completeness guard
(8/8); (4) long_session marker (11/11 + live boot validation); (5) this consolidation. All self-checked;
stack STOPPED; evening cron at 20:00 is clear and will live-fire both G15 (eQ21 rerun) and G16 (pre-flight
on a healthy key).

### Commit draft (consolidated, matching the repo's commit style; Alkama commits — I never do; no co-author line)

Covers ALL currently-uncommitted tracked work (spans 07-02 → 07-06):

```
Interface artist-pass + A2 long_session/LLM remarks + G15 guard + drills 07-02..07-06

Interface (Layout.jsx/useClara.js/index.css + UI_AUDIT_2026-07.md): two live
recording bugs + edge-case sweep — single-socket reconnect (no duplicate
streams), /history merge-order fix, bare-number ordered-list sanitizer,
rAF-batched streaming + per-message buffers, React.lazy code-split, escalation
mode chip (FAST->DELIBERATE via new mode events in agent.py/api.py), thought
-stream hover readability, ambient card vote->dismiss, textarea a11y, 8MB
upload ceiling (client notice + server-side WS belt in api.py).

A2 ambient (ambient_loop.py/salience.py): LLM-generated remark polish
(AsyncOpenAI, template fallback), 72h remark_seed dedup, humanized templates,
per-class character registers (odd_hours night-owl tease) + numeric-fidelity
backstop (_remark_fidelity_ok — polish may not invent or round facts);
NEW long_session marker — window-evaluated over A0's change-only timeline,
idle>=15min breaks, fires at 150min continuous, dominant-app nudge, env-tunable
knobs (LONG_SESSION_TRIGGER_MIN/BREAK_MIN);
NEW off_rhythm marker — 15-min engaged-span dominance (>=60%, idle uncredited)
by an app off-baseline for the hour (recognition >=0.75 to surface), with the
still-drifting guard (self-correction suppresses; no baseline = silent);
knobs OFF_RHYTHM_WINDOW_MIN/OFF_RHYTHM_DOMINANCE.

G15 (agent.py): date_time completeness guard in _run_fast — format_llm
condensed a computed date block to "Wednesday." (05e Q21); formatted response
must preserve the computed target date or raw output ships.

Drills: reports 07-02e..07-06m analyzed; 07-03 29-question rotation verdict =
ladder HELD (substance-verified); 07-06m = INFRA casualty (DeepSeek 402, late
cron) — fail_counts restored, Telegram alert loop proven end-to-end.

.gitignore: AGENT_ZERO_PRIVATE/ + My_Schedule/ (private, never committed).
```

*(If he prefers per-feature commits: interface+api.py+agent.py-mode-hunks / ambient pair / agent.py-G15-hunks
/ reports+docs+.gitignore — TIMELINE.md + BACKLOG.md carry hunks for all four, stage per-hunk or fold.)*

### ⚠️ Load-bearing work that is GITIGNORED (R11 — will NOT appear in `git diff`)
- **`tests/test_harness.py`** — **G16 `api_is_usable()`** (the 402 pre-flight). Live only in the working
  tree; tonight's cron depends on it behaving.
- **`tests/questions_morning.json`** — the 14-question fail_count restore after the 402 pollution.
- **`tests/test_date_completeness.py`** (8 cases) + **`tests/test_long_session.py`** (11 cases) — the
  regression guards for G15 + long_session.
- **`tests/verification.py`** — G4 claimed-line check + the `"..."`-span extractor fix (missed-PASS class);
  **`tests/test_verification.py`** fixture suite 41→46. Live only in the working tree; the cron exercises it.
- **`tests/coherence_dialogues.json`** — the G14 write-safe rephrase of `manager-her` T2.
- **`briefs/BRIEF_53`** (key_facts false-PASS, awaiting decision — rolled×3) + **`briefs/BRIEF_54`**
  (partner A pilot: §7.1 API contract, §7.2 re-registration checklist for his Monday task).
R11 (un-ignore tests/+briefs/ vs keep-local) remains an open policy call.

### Open items awaiting Alkama (async — staged in My_Schedule/06jul_Schedule.md)
- **Stephen call** — thread check; Monday was proposed but never confirmed on record.
- **partner A re-registration** — BRIEF_54 §7.2; the moment a fresh agent key exists I build the thin
  adapter (same-day, agreed).
- **LinkedIn sends ×3** — Michael relevance-answer, Jesse, the governance partner drafts ready in LINKEDIN_CONVOS.md.
- **Rot-rule choices (rolled×3)** — BRIEF_53 decision + the commit itself (draft above).
- **R19** — Telegram-voice phone test (2 min, his phone required).
- **BRIEF_55 / R18** *(new, Task 7)* — test-mode write-DENY via the admissibility gate (G14 durable fix;
  gate's first enforcement = arming call). Recommends Option A; ~30-line diff, built same-day on the nod.

*(loop remains Ongoing — resume on "continue")*

---

## Task 6 — Y · odd_hours characterful-prompt upgrade (A2 remark composer)

**Which & why (before):** The five announced tasks are done and the loop continues per contract. Of the
queued candidates (off_rhythm build — sizeable, next agreed A2 marker; G14 coherence-leak — diagnosis-only;
this), I pick the characterful upgrade because it's the SMALLEST agreed item still open, it directly answers
Alkama's calibration feedback (his own words set the target register: "What are you doing so late, night
owl?" — playful, personal), and it composts perfectly with Task 4: long_session nudges will route through
the same composer, so better character pays twice immediately. Scope: ONE function's prompt
(`_llm_remark` in ambient_loop.py) gains a per-class character direction (odd_hours = playful night-owl
tease, one light rhetorical question explicitly allowed there — his example IS a question; long_session =
warm stretch/water care, never nagging; new_app = curious; battery = dry + urgent). Template fallback and
all failure semantics untouched — a prompt change cannot break the loop. Edge cases: unknown class → generic
instruction; keep facts-exact/12-hour/no-emoji constraints; keep length bound. Validation: module self-test
(deterministic paths) + a live offline sample of all four classes through the real API (topped up; no
backend needed; 13:30 vs 20:00 cron — safe).

**Outcome — Mon 06 Jul, ~13:50 — DONE, and the live sample earned its keep.** First pass: register landed
perfectly on odd_hours ("What are you doing in VS Code at 4 AM?...") but the sampling caught **temp-1.1
FABRICATION** on the other classes — invented facts ("Two new Obsidian notes appeared on your desktop",
"You checked the battery at 1:15 PM") and a rounded number ("~2.7h" → "Three hours"). Same failure class
as the FAST formatter's old 65536→65636. Fixed twice over: (1) prompt hardened (never add/round a fact);
(2) **`_remark_fidelity_ok`** deterministic backstop — every template number must survive verbatim into
the polish, no new clock-times allowed, violation → template ships (mirrors `_run_fast`'s numeric guard).
Re-sample: all four classes clean WITH character ("VS Code at 4 AM — what are you doing up at this hour,
night owl?"; "Been at it about 2.7 hours straight in VS Code — time for a stretch and some water").
Guard cases 5/5, `--selftest` green. Fork decision logged: fidelity > flourish — when the guard trips, the
generic-but-true template ships rather than retrying the LLM (bounded cost, and the drill's whole history
says trust the deterministic path). Files: core_logic/ambient_loop.py; TIMELINE [ENHANCEMENT]; BACKLOG
Y1-remark-character struck.

---

## Task 7 — G14 · Coherence-drill FILESYSTEM leak (investigation, diagnosis-first)

**Which & why (before):** Last substantial 🟢 on the board that needs no Alkama input to START (off_rhythm
is the bigger 🟡 but is a multi-hour build — wrong pick with an unknown remaining window; G14's diagnosis
phase is bounded and its output decides which of three fix options is even right). The bug matters beyond
hygiene: a fictional-fixture turn WROTE A REAL FILE (`deadlines.md`, "requested by Priya" — the drill's
invented manager) during the 07-01 morning cron. `memory_mode=ephemeral` isolates memory but tool calls
still execute — the same fixture-pollution class as 06-07, now via the filesystem, and nothing stops a
future dialogue from overwriting a REAL file. Plan: (1) session log `2026-07-01_08-00-55.log` → find the
turn + tool call that wrote it; (2) read the offending dialogue in `coherence_dialogues.json`; (3) inspect
`deadlines.md` itself; (4) weigh the three fix options (write-safe phrasing / tool-sandbox / assert-not-
execute) and either apply the contained one or brief if it touches the harness contract. Diagnosis first,
fix only if contained.

**Outcome — Mon 06 Jul, ~14:20 — DONE (mitigated + briefed).** Mechanism nailed from the log:
`manager-her` turn 2 ("She also wants the API spec by Friday.") → Interpreter read it as a REAL request
(`requires_planning=true`) → DELIBERATE → `tool_search("write file…")` → `write_file` → real `deadlines.md`
with fictional Priya content. Clara behaved CORRECTLY — the fixture was simply indistinguishable from a
real ask; ephemeral mode isolates memory, not tool execution. Actions: (1) **contained fix applied** — turn
2 rephrased informational ("She also mentioned the API spec is due Friday — just so you have the context.");
Priya/Friday facts intact, scored probe untouched, JSON valid, scorer self-test **24/24**; all other setup
turns audited declarative-clean (noted: the `ambiguous-service` control references "this repo" — a
misbehaving run could touch real files; covered by the class fix). (2) **`deadlines.md` deleted** —
post-investigation cleanup per the backlog's own "not until investigated"; content preserved verbatim in
the session log, BRIEF_55, and here; its .gitignore line KEPT as defense. (3) **Durable class-fix =
BRIEF_55** (`briefs/BRIEF_55_TestMode_Tool_Sandbox.md`, number verified free): recommended Option A —
thread `memory_mode` into the tool envelope and let the ADMISSIBILITY GATE enforce one rule (test-mode +
write-class → DENY with an explanatory result). It would be the gate's first live enforcement — an arming
call, hence 🔴-briefed (queued as R18). Files: tests/coherence_dialogues.json (gitignored per R11),
briefs/BRIEF_55 (gitignored), TIMELINE [FIX], BACKLOG G14 struck + R18 added.

---

## Task 8 — G4 (remaining) · claimed-line verification for verbatim quotes

**Which & why (before):** The last free-to-start 🟢 with real drill leverage. G4's open tail is exactly a
documented Layer-1 v1 gap (CLAUDE.md: "Verbatim PASS confirms the quote is real, not that the answer is
correct") — today an answer can quote a REAL line but claim a FABRICATED line number and the scorecard
PASSes it silently; I only catch it by hand-spot-check. Extension: when the answer claims a location
("line 42" / "file.py:42") for a quote `v_verbatim_quote` matched, locate the quote's actual line(s) in
the source and require a claimed number within ±3 (absorbs same-day edit drift between the cron run and my
grade-time read — the file is re-read at grade time and CAN have moved). Mismatch → **UNVERIFIABLE with an
explicit location-mismatch reason** (routes to my manual judgment), NEVER a FAIL — the trust-safe contract
(quote real + line drift ≠ proven fabrication) and the 06-01 false-FAIL lesson both forbid confident
negatives here. No claimed line → behavior unchanged (strictly additive). Multi-line quotes → skip the
location check (can't single-line-locate; today's PASS stands). 🟢 verifier work, deterministic,
self-tested: new fixture cases in test_verification.py (both directions: right line → PASS, wrong line →
UNVERIFIABLE, no line → PASS) + the full existing fixture suite must stay green (the Phase-1.4 pre-flight
guards it every cron anyway). Risk: over-eager line-claim extraction (times like "08:10", counts) — the
regex anchors to "line N" wording and file:line forms only.

**Outcome — Mon 06 Jul, ~14:45 — DONE, plus a second real gap found and fixed.** (1) Claimed-line check
built as designed: `_claimed_line_numbers` (narrow: "line(s) N[-M]" wording + ".ext:N" form only — a time
like "08:10" can't match) + `_quote_line_locations`; mismatch → UNVERIFIABLE with evidence "quote IS
verbatim … but at line(s) [4], answer claims [42] — possible fabricated line number". (2) **Bonus catch:**
first fixture run FAILED 3 cases and exposed that `_extract_quote_candidates` never handled plain
`"..."`-quoted spans mid-sentence (only backticks/bold) — Clara's common `Line 4: "…"` answer format could
NEVER confirm → silent missed-PASS class (also «»/curly variants, eQ17's style). Extractor extended;
trust-safe direction (adds PASS capability only). Self-test 41→46 fixtures, **46/46**; both directions
live-verified on a throwaway fixture. The cron's Phase-1.4 pre-flight now guards the new behavior daily.
Files: tests/verification.py, tests/test_verification.py (both gitignored per R11 — flagged in the
consolidation's R11 list), TIMELINE [UPDATE], BACKLOG **G4 fully closed**.

---

## Task 9 — G1 · Drill: 2026-07-06-evening (the G15/G16 live-fire run)

**Which & why (before):** Mid-scoping the G7 watch-item I checked the clock: it's ~21:45 — the 20:00
evening cron ALREADY RAN (report stamped 20:10, port 8001 clean — the harness tore down properly). An
unanalyzed report is the standing top-🟢 and this one carries the day's whole validation story: (a) **G16's
first scheduled outing** — the run happening at all means `api_is_usable()` passed on the topped-up key;
(b) **G15's live fire** — eQ21 (the 05e "Wednesday." formatter-condensation FAIL, fail_count=1) reran
tonight WITH the completeness guard in `_run_fast`; (c) first evening run over the code shipped this
window (long_session classifier live in the booted backend's A2 loop). Timing note for honesty: the cron
ran against the ~20:00 working tree — G15/G16/long_session were in it (built before 13:15); Tasks 6-8
(remark character, dialogue rephrase, verifier claimed-line) landed after and ride the NEXT run. Protocol
as always: scorecard-anchored, ground-truth every FAIL from the session log before judging Clara, analysis
into the report file (the durable record), promotions per the ladder.

**Outcome — Mon 06 Jul, ~22:10 — DONE. 22/22 clean, and the day's builds validated themselves.**
(1) **G15 FIRED LIVE**: format_llm condensed the Q21 date_time block AGAIN (2/2 evenings = persistent
formatter behavior, guard is load-bearing) — log 20:08:54 shows the guard shipping the raw block; verifier
PASS. eQ21 fail_count → 0. (2) **G16 first scheduled pass** — the run existing = pre-flight passed on the
refilled key. (3) All 4 UNVERIFIABLEs manually confirmed (Q17 verbatim-exact at agent.py:1929; 3 knowledge
sound); key_facts substance spot-checked (Q2/Q13/Q20 exact). (4) Layer-2 gold-seed MATCHED — the 05e miss
was a one-off. (5) **13 questions CLIMB-DUE flagged by the harness — NOT climbed** (fork decision:
rotation stays on Alkama's explicit trigger per CLAUDE.md; a second full climb 3 days after the 07-03
batch would outpace validation value; the list is queued in the report's ladder section). Analysis written
into the report (checker: ANALYZED); questions_evening.json states updated (all pass/fc=0, meta stamped);
TIMELINE [UPDATE]. Note for tomorrow: the 46-fixture verifier engine (Task 8) gets its first cron outing.

---

## Task 10 — G7 (remaining) · over-strict key_facts watch-item close-out

**Which & why (before):** The original Task-9 pick before the evening report superseded it — resuming it
now because it's the last S-size 🟢 with a clean close condition. G7's open tail is a WATCH: "any
over-strict key_facts class flagged in recent drills" (a TIMELINE note flagged the class around the
06-27/06-28 window). Close condition is evidence-based: trace the original flag to its TIMELINE/report
context, then check every key_facts verdict in the runs since (07-01 → tonight) for a repeat of the
over-strict pattern (a correct answer failed because must_include terms were phrased differently). If the
class hasn't recurred across the rotated set's 7 runs — which use key_facts heavily (7-8 questions/run) —
the watch closes as not-reproducing; if it has, it becomes a concrete verifier fix. Tonight's run alone
had 8 key_facts PASSes with zero strictness complaints, so I expect close-as-clean, but the check must
walk the actual reports, not vibes. Small, deterministic, bounded.

**Outcome — Mon 06 Jul, ~22:25 — DONE, and the watch turned out to be earning its keep.** The class never
recurred in a graded run — but it wasn't dead, it was LATENT: (a) `v_key_facts` FAILs a 2-fact oracle on
ONE unasserted fact (`(len+1)//2` = 1 for len 2), and (b) a mechanical scan found **5 current questions**
whose must_include demands a term the question itself supplies (mQ16 'admissibility_ledger', eQ02 'warmup',
eQ03 'GEMINI_API_KEY', eQ06 '0.35', eQ20 'correct') — each one false-FAIL bait for a correct answer that
answers without echoing. Defused oracle-side (no question text touched): every flagged group broadened
with SUBSTANCE synonyms (the asked-for content now satisfies the oracle — e.g. eQ06's "why" terms
"event loop"/"blocking"/"cpu-bound" alongside "0.35"). Monotone-safe (any-of groups only gain paths).
Validated: tonight's four real answers PASS under the new oracles; a synthetic correct-non-echo eQ06
answer flips FAIL-risk→PASS; verifier suite 46/46. **G7 fully closed** (the Tier-2 retry half closed
06-27). Files: tests/questions_{morning,evening}.json (gitignored per R11), TIMELINE [UPDATE], BACKLOG.

---

## Task 11 — Y1b · off_rhythm marker (the agreed second A2 signal build)

**Which & why (before):** The board's remaining 🟢 S-items are gone (G4/G7/G14 closed today); the next
agreed build is off_rhythm — Alkama greenlit it in the 07-04 calibration ("off rhythm... let's build it")
with the algorithm settled on paper. I recovered the AGREED design verbatim from the session transcript
before building (not from memory): (1) **window-dominance** — over ~15 min, one app must hold the
MAJORITY of held-span ("Instagram 11 min, Brave 3, VS Code 1" fires; a 10-second switch NEVER does — his
explicit worry); (2) **hour-deviance** — the dominant app must be off-baseline for THIS hour
(proc_hour_days recognition), so lunch-hour Brave never fires; (3) **the still-drifting guard** — at
fire-time the CURRENT foreground must still be the deviant app, else total suppression ("no nagging the
self-corrected" — his confirmed requirement, "If I self corrected... never taken and I'm not nagged
right?" → yes); (4) screenshots are strictly downstream of a committed fire and the capture pipeline is
PARKED (vision backend undecided) — so this build is the text-only decision layer through the existing
gate/dedup/ledger/composer path, exactly like long_session. 🟡-contained: additive detector in
salience.py + tick() wiring + composer branch; A2_MODE already gates surfacing; env-tunable knobs
(OFF_RHYTHM_WINDOW_MIN=15, OFF_RHYTHM_DOMINANCE=0.6). Edge cases to lock in tests: sparse A0 (foreground
persists), idle spans don't count toward dominance, currently-idle → silent, self-corrected → silent,
familiar-at-this-hour app → silent, immature baseline → silent, midnight-adjacent hours.

**Outcome — Mon 06 Jul, ~23:00 — DONE (the lifetime's final task; Alkama called the graceful stop
mid-build).** `detect_off_rhythm` in salience.py implements the three agreed gates exactly as recovered
from the transcript: engaged-span dominance (idle credits no app), hour-deviance recognition feeding the
gate's EXISTING off_rhythm novelty branch (rhythm_dev — SURFACE needs ≥0.75, i.e. <25%-of-days
familiarity; no baseline → total silence), and the still-drifting suppressor (current foreground must
still be the deviant app, user not idle — self-correction invisible, and since capture is downstream of
a committed fire, "no fire → no camera" holds structurally for the future screenshot pipeline). Wired
through the identical tick() path with a 2h class cooldown + off_rhythm added to _AMBIENT_COOLDOWNS;
gentle anchor-register for the LLM polish; template fallback. `tests/test_off_rhythm.py` **15/15** (one
test-authoring bug caught by the suite itself: `now=ep(60)` crossed into hour 15 where the synthetic
baseline had no entries — re-anchored to ep(59)); salience/ambient_loop/long_session suites all green.
NOT boot-tested live (23:00 — post-cron, and the class ships through the already-boot-validated A2 loop;
tomorrow's 08:00 cron boots it naturally — accepted, named risk, same call as G3's). Files:
core_logic/salience.py, core_logic/ambient_loop.py, tests/test_off_rhythm.py (gitignored per R11).
