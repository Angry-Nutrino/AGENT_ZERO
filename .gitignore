# --- SECRETS (NEVER TOUCH) ---
.env
.env.local
interface/.env
api_keys.txt
.claude/

# --- PYTHON ---
__pycache__/
*.py[cod]
*$py.class
venv/
.venv/
env/
Lib/
Scripts/
briefs/
tests/

# --- NODE / REACT (CRITICAL ADDITION) ---
node_modules/
**/node_modules/
interface/dist/
interface/build/
.eslintcache

# --- AI MODELS & WEIGHTS ---
core_logic/moondream_brain/
**/moondream_brain/
core_logic/models/
*.pt
*.pth
*.bin
*.onnx
*.safetensors

# --- MEMORY & DB (personal data — NEVER track) ---
core_logic/memory.json
core_logic/ambient.json
core_logic/ambient_patterns.json
core_logic/.ambient.json.*
core_logic/ambient_shadow.jsonl
core_logic/ambient_loop_state.json*
core_logic/ambient_ledger.json*
core_logic/.ambient_loop_oneshot.tmp
core_logic/ambient_screen.json
core_logic/.ambient_screen.*
a3_screen_*.png
core_logic/memory.json.bak-*
core_logic/memory.json.corrupt-*
core_logic/.memory.json.*
core_logic/archived_vault_facts.json
faiss_index.bin
faiss_index.pkl

# --- HF MODEL CACHE (repo-local redirect; large model weights — never track) ---
.hf_cache/

# --- CONVERSATION STORE (real voice/interface/telegram/WhatsApp message content — personal) ---
conversations/

# --- WHATSAPP WEB SESSION (a LIVE login — leaking it = account hijack) + puppeteer cache ---
whatsapp_service/.wwebjs_auth/
whatsapp_service/.wwebjs_cache/
*.db
core_logic/tasks.db-shm
core_logic/tasks.db-wal
core_logic/tasks.db.bak-*
*.sqlite3
clara_backend.pid
# (the old ../CLARA_PERSONA entries were broken — gitignore patterns can't use ../;
#  these are the working forms for the repo-root persona files)
CLARA_PERSONA.md
CLARA_PERSONA_CONDENSED.md

core_logic/knowledge_base/
core_logic/docs/
core_logic/photos/
photos/
ledger_test_file.txt

# --- TRACES ---
traces/

# --- TEMP FILES (Specific, not global) ---
# Only ignore the temp inputs, not your UI assets
temp_*.png
temp_*.jpg
temp_*.wav
temp_doc_*
temp_image_*
output.wav
*.log

# --- OLD/JUNK ---
jarvis_v2/
.DS_Store
Thumbs.db
style.md
core_logic/admissibility_ledger.json
core_logic/.*_key
deadlines.md
AGENT_ZERO_PRIVATE/
*_demo/
My_Schedule/
drill_workspace/

# Governance audit sweep reports (BRIEF_57 / R20) — private deal artifacts
governance_audit_reports/

# Work logs that name design partners and private business context — never publish.
busy-mode-reports/
*_pilot_call.py

# Consolidated contact/context dump — real third-party names + private business discussion.
# NEVER commit (repo is public). Keep alongside AGENT_ZERO_PRIVATE/.
Alignment.md

# ── Narrative / process logs — UNTRACKED 2026-08-13 (privacy) ────────────────────
# These narrate real interactions with real people and are where every leak to date
# has happened: an org name in TIMELINE, a person's name x3 in BACKLOG, and two more
# introduced the same day they were being audited. reports/ additionally published
# Alkama's OWN activity data (ambient questions print which apps he used, how often,
# and which hours he was active) into a public repo.
# The public repo keeps the PRODUCT: code + CLAUDE.md. Process logs stay local.
TIMELINE.md
BACKLOG.md
reports/
